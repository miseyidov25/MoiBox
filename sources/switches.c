#include "switches.h"

void switches_init(void) {
    // pins would be configured as inputs with pullups
}

uint8_t switches_read(void) {
    // for now return 0; later read hardware pins
    return 0;
}

